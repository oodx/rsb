RSB Tasks Tracker
Date: 2025-09-15T00:00:00Z

Completed
- [x] Rename internal logger to `stderrx` (shim for `glyph_stderr`) and update call sites
- [x] Enhance `pre_dispatch!` to auto-register handlers and accept `desc:` (mirror `dispatch!`)
- [x] Integrate progress module behind `progress` feature; add `FEATURES_PROGRESS.md`; restore two-mode rate formatting
- [x] Rewrite `FEATURES_COLORS.md` for MODERN + SPEC_ALIGNED
- [x] Refresh README: Start Here table, Feature docs with Progress, Cargo features section
- [x] Refresh docs index: tables and relative links
- [x] Remove stashed `set_var!`/`get_var!` macros; use function API in tests/docs
- [x] Implement grouped + per-dependency `rsb::deps` surface; add `deps::prelude` (optional)
- [x] Validate tests: core, visuals, and progress builds

Planned / Next
- [ ] Legacy macro migration: move remaining legacy macros in `src/macros/` into module-owned `macros.rs`; update `prelude::macros`
- [ ] Optional: convert math `random_list!` error logging to `stderrx` for uniformity
- [ ] Optional CI: add lanes for smoke, visuals, progress
- [ ] Keep README and docs index in sync when adding new features/modules
- [ ] RFC guideline: require brief discussion before changing user-visible output defaults (e.g., progress formatting)

Notes
- Visuals are opt-in; default builds print plain text with color tags stripped.
- Deps surface: enable individual crates with `deps-<name>` or the full set via `deps`/`deps-all`.
